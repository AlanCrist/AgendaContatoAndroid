<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="25dp"
    android:orientation="vertical"
    android:padding="25dp"
    tools:context=".activity.MainActivity">

    <Button
        android:id="@+id/btnDiscador"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="abrirDiscador"
        android:text="Discador" />

    <Button
        android:id="@+id/btnContatos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="abrirContatos"
        android:text="Contatos" />

    <Button
        android:id="@+id/btnNumerosDiscados"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="abrirNumerosDiscados"
        android:text="últimas discagens" />

</LinearLayout>